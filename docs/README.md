# Documentation Index

Architecture and internals documentation for this ERPNext repository. Every entry carries line-level citations back into the source.

Start with [architecture/overview.md](architecture/overview.md) if you are new. Use this index as a lookup map; use each page's `Key files` section to jump into the code.

## Getting Started

- [Getting Started index](getting-started/README.md) — section overview + 5-minute quick start.
- [Prerequisites](getting-started/prerequisites.md) — versions (Python 3.14, Node 24, MariaDB 10.6 / Postgres, Redis, wkhtmltopdf 0.12.6), apt packages, pre-commit toolchain.
- [Installation](getting-started/installation.md) — `bench init`, `bench get-app payments && bench get-app erpnext`, `bench new-site`, `install-app`, `after_install` walkthrough, `site_config.json` reference.
- [Development](getting-started/development.md) — `bench start`, Procfile processes (`redis_cache` / `redis_queue` / `redis_socketio` / `socketio` / `watch` / `schedule` / `web` / `worker`), port `:8000`, DB access, migrations.
- [Testing and quality](getting-started/testing-and-quality.md) — `run-parallel-tests`, `run-tests --module` / `--test`, `ERPNextTestSuite`, `tearDown` rollback, `allow_tests`, pre-commit (Ruff + Prettier + ESLint), commitlint, `develop`-commit block.
- [Operations](getting-started/operations.md) — CLI cheatsheet, `site_config` / `common_site_config` keys, production overview, troubleshooting catalogue, debugging tools, jump-points into architecture docs.
- [`after_install` seed catalogue](getting-started/after-install-seed.md) — exhaustive line-by-line walkthrough of `erpnext/setup/install.py:after_install`: 18 ordered seed steps (Analytics role, Single defaults + Currency Exchange Settings, repost defaults, Print Settings + UTM Campaign custom fields, multi-tenant Email Account/Communication links, default Success Actions, Incoterms, 5 default Role Profiles, navbar items, app name, Customer/Supplier desk-access strip, default Operations, pegged Gulf currencies, default print formats, letter heads). Also enumerates what `after_install` does **not** seed (Company / CoA / warehouses / cost centers / departments / country fixtures / demo data — all bootstrapped later by `Company.on_update` or the Setup Wizard).

## Architecture

- [Overview](architecture/overview.md) — module map, where-to-start-tracing cheatsheet, conventions.
- [Controller hierarchy](architecture/controllers.md) — `StatusUpdater → AccountsController → StockController → SellingController / SubcontractingController → BuyingController`, with per-layer responsibilities.
- [DocType pattern](architecture/doctype-pattern.md) — schema JSON + Python controller + JS + tests, and how transaction DocTypes plug into the controller chain.
- [DocType lifecycle](architecture/doctype-lifecycle.md) — `validate → before_save → on_submit → on_cancel → on_trash` and how each controller layer contributes.
- [Hooks and overrides](architecture/hooks-and-overrides.md) — complete tour of `erpnext/hooks.py`: `doc_events`, `scheduler_events`, `regional_overrides`, `extend_doctype_class`, portal routes, boot, metadata groupings.
- [Scheduler jobs catalogue](architecture/scheduler-jobs.md) — every entry in `scheduler_events` (3 cron + 1 hourly + 6 hourly_maintenance + 30 daily_maintenance + 1 weekly + 2 monthly_long): function path, owning module, what it touches, what fails if it stops, cross-links to module/flow docs.
- [Hooks catalogue (per-DocType registries)](architecture/hooks-catalogue.md) — the 17 list/dict registries in `hooks.py` (`period_closing_doctypes`, `auto_cancel_exempted_doctypes`, `accounting_dimension_doctypes`, `repost_allowed_doctypes`, `bank_reconciliation_doctypes`, `subscription_doctypes`, `invoice_doctypes`, `advance_payment_*_doctypes`, `communication_doctypes`, `treeviews`, `calendars`, `website_generators`, `naming_series_variables`, `default_log_clearing_doctypes`, `ignore_links_on_delete`, `additional_timeline_content`) with consumer code paths and semantics.
- [Boot session and bootinfo injection](architecture/boot-session.md) — `boot_session` (sysdefaults, Companies pre-load, party-account-types, current fiscal year), `extend_bootinfo` (SLA doctypes + employee link), `notification_config` (33 DocType counters + Company sales target), `leaderboards` (Customer / Item / Supplier / Sales Partner / Sales Person), `filters_config` (Fiscal Year custom filter), `get_help_messages`, `on_session_creation`. Includes a per-login sequence diagram.

## Patterns

- [Regional overrides](patterns/regional-overrides.md) — `@erpnext.allow_regional` decorator + `regional_overrides` registry + `erpnext/regional/<country>/` layout.
- [Patches](patterns/patches.md) — `patches.txt` manifest, `pre_model_sync` vs `post_model_sync`, how `bench migrate` consumes them.

## Flows

- [Accounting flow](flows/accounting-flow.md) — end-to-end GL write path: `make_gl_entries`, `process_gl_map`, `save_entries`, `make_reverse_gl_entries`, round-off, freeze, PCV guard. Mermaid diagrams for Sales Invoice submit and cancel.
- [Taxes and totals](flows/taxes-and-totals.md) — `calculate_taxes_and_totals` lifecycle: item values, inclusive/exclusive taxes, item-wise breakup with error diffusion, rounding, and tax-row → GL mapping.
- [Payments flow](flows/payments-flow.md) — Payment Entry `validate`/`on_submit`/`on_cancel`, five-composer GL build, allocations / advances / FX gain-loss / deductions, Payment Reconciliation, `set_payment_schedule`.
- [Stock flow](flows/stock-flow.md) — SLE write path (`make_sl_entries` → `update_entries_after` → `update_bin_qty`), valuation branches (FIFO/LIFO/Moving Average/Batch/Serial), negative-stock gating, backdated reposting, perpetual GL bridge, rounding diff for internal transfers. Mermaid for Purchase Receipt / Delivery Note / Stock Reconciliation submit + cancel.
- [Selling flow](flows/selling-flow.md) — Quotation → Sales Order → Delivery Note → Sales Invoice cascade: `status_updater[]` contract, `update_prevdoc_status`, `billed_amt` FIFO redistribution, SO close / hold / re-open, drop-ship SO→PO→PR reverse lane, stock-reservation consumption, inter-company mirror. Mermaid sequence diagrams per stage.
- [Buying flow](flows/buying-flow.md) — Material Request → RFQ → Supplier Quotation → Purchase Order → Purchase Receipt → Purchase Invoice cascade: `status_updater[]` contract, `update_prevdoc_status`, PO→SO drop-ship reverse lane, `update_billed_amount_based_on_po` FIFO redistribution, Stock Received But Not Billed bridge, Landed Cost Voucher revaluation, subcontracting branch to Subcontracting Order, internal-transfer `sales_incoming_rate`, return flow, inter-company mirror. Mermaid sequence diagrams per stage.
- [Subcontracting flow](flows/subcontracting-flow.md) — both coexisting flows: legacy PO/PR-embedded (`is_old_subcontracting_flow=1`) with `Purchase Order Item Supplied` / `Purchase Receipt Item Supplied` and new SCO/SCR (`Subcontracting Order` → `Subcontracting Receipt`). Covers `SubcontractingController` dispatch (`subcontract_data`), BOM explosion vs material-transferred backflush, `make_rm_stock_entry`, supplier-warehouse SLE + GL, SCR cancel, and the inward customer-provided (`Subcontracting Inward Order`) mirror. Mermaid sequence diagrams per branch.
- [Manufacturing flow](flows/manufacturing-flow.md) — Production Plan → Work Order → Job Card → Stock Entry cascade. Covers BOM cost roll-up + explosion, sub-assembly branching (In House / Subcontract / Material Request), auto-creation of WOs / MRs / POs, Work Order status lifecycle (Not Started → In Process → Completed / Stopped / Closed), Job Card time-log + complete writeback to Work Order Operation, Stock Entry manufacturing purposes (`Material Transfer for Manufacture`, `Manufacture`, `Material Consumption for Manufacture`, `Disassemble`), BOM Update Log level-wise cost propagation cron. Mermaid sequence diagrams per stage.
- [Assets flow](flows/assets-flow.md) — three asset-side flows with mermaid sequence diagrams: depreciation posting (daily scheduler → `Asset Depreciation Schedule` row → JE per due row → Dr Depreciation Expense / Cr Accumulated Depreciation, schedule-row `journal_entry` stamping), capitalization (Asset Capitalization SLE for consumed stock + GL bridge for consumed assets/services, target-asset value bump), repair (always-issued child Stock Entry plus the `capitalize_repair_cost=1` GL bridge that posts Cr PI expense + Cr SE expense / Dr Fixed Asset and bumps finance-book life).
- [Customer-portal flow](flows/customer-portal-flow.md) — login → `on_session_creation` (portal.utils.create_customer_or_supplier auto-provisioning) → role assignment via `set_default_role` → sidebar render from `standard_portal_menu_items` → list/detail per `website_route_rules` → record-scoping via `has_website_permission` (controllers/website_list_for_contact.py) → public contact form Lead/Opportunity/Communication composer (`override_whitelisted_methods` → templates/utils.send_message). 4 sequence diagrams.

## Modules

- [Accounts](modules/accounts.md) — module layout, Chart of Accounts (`root_type`/`account_type`, company scoping), accounting dimensions, cost centers, fiscal year / accounting period, tax framework, bank clearance, reports, scheduler jobs, regional hooks.
- [Accounts DocType reference cards](modules/accounts-doctypes.md) — per-doctype cards for Sales Invoice, Purchase Invoice, Journal Entry, Payment Entry, POS Invoice, Period Closing Voucher.
- [Stock](modules/stock.md) — module layout (`stock_ledger.py`, `serial_batch_bundle.py`, `valuation.py`, `utils.py`, `reorder_item.py`, `stock_balance.py`), SLE model + Bin, Serial and Batch Bundle, Warehouse tree, Item stock flags, perpetual vs periodic, valuation math, scheduler jobs, stock-related metadata registries.
- [Stock DocType reference cards](modules/stock-doctypes.md) — per-doctype cards for SLE, Bin, SABB, Delivery Note, Purchase Receipt, Stock Entry, Stock Reconciliation, Material Request, Pick List, Packing Slip, Landed Cost Voucher, Quality Inspection, Stock Reservation Entry, Repost Item Valuation, Stock Closing Entry / Balance, Warehouse, Batch, Serial No.
- [Selling](modules/selling.md) — Selling-module overview: directory layout, `SellingController` responsibilities (validations, rate floor, product-bundle expansion, target warehouse, `set_incoming_rate`), customer + credit-limit plumbing, Selling Settings knobs, scheduler jobs (`set_expired_status`), reports, POS pages, regional hooks.
- [Selling DocType reference cards](modules/selling-doctypes.md) — per-doctype cards for Quotation, Sales Order, Delivery Note, Sales Invoice, POS Invoice, POS Profile, POS Opening / Closing Entry, Customer, Customer Group, Sales Person, Sales Team, Sales Partner, Sales Taxes and Charges Template, Pricing Rule, Promotional Scheme, Shipping Rule, Blanket Order, Product Bundle, Stock Reservation Entry, Installation Note, Selling Settings.
- [Buying](modules/buying.md) — Buying-module overview: directory layout, `BuyingController` responsibilities (valuation-rate composition, asset auto-creation, internal-transfer `sales_incoming_rate`, legacy subcontracting supplied-items), `SubcontractingController` contribution, Supplier + Supplier Scorecard, Buying Settings knobs, scheduler jobs (`refresh_scorecards`, `set_expired_status`, `reorder_item`), reports, regional hooks (UAE RCM).
- [Buying DocType reference cards](modules/buying-doctypes.md) — per-doctype cards for Material Request, Request for Quotation, Supplier Quotation, Purchase Order, Purchase Receipt, Purchase Invoice (buying-side notes), Supplier, Supplier Group, Supplier Scorecard + sub-DocTypes, Landed Cost Voucher, Purchase Taxes and Charges Template, Buying Settings, Purchase Order / Receipt / Invoice Item and Supplied-Item child tables.
- [Subcontracting](modules/subcontracting.md) — module layout, `SubcontractingController` responsibilities (flow dispatcher via `subcontract_data`, BOM explosion vs transferred backflush, supplier-warehouse SLE / GL, `make_rm_stock_entry`), `SubcontractingInwardController` summary, Buying Settings knobs, scheduler jobs (none), regional hooks (none), cross-links to legacy `buying/` supplied-item child tables.
- [Subcontracting DocType reference cards](modules/subcontracting-doctypes.md) — per-doctype cards for Subcontracting Order, Subcontracting Receipt, Subcontracting BOM, Subcontracting Inward Order, SCO Item / Service Item / Supplied Item, SCR Item / Supplied Item, legacy Purchase Order Item Supplied / Purchase Receipt Item Supplied, Stock Entry subcontracting purposes.
- [Manufacturing](modules/manufacturing.md) — Manufacturing-module overview: directory layout, controller posture (no controller — all four lifecycle DocTypes sit on `Document`), master data (BOM + children, Routing, Operation, Workstation + Workstation Type), planning (Production Plan, Master Production Schedule, Sales Forecast), execution (Work Order, Job Card), Manufacturing Settings knob reference, scheduler jobs (`resume_bom_cost_update_jobs` cron, `auto_update_latest_price_in_all_boms` daily), cross-module interactions, regional hooks (none).
- [Manufacturing DocType reference cards](modules/manufacturing-doctypes.md) — per-doctype cards for BOM + 6 children, BOM Creator + child, BOM Update Log / Batch / Tool, Routing, Operation + Sub Operation, Workstation + Workstation Type + 4 children, Plant Floor, Production Plan + 8 children, Master Production Schedule + child, Sales Forecast + child, Work Order + Work Order Item + Work Order Operation, Job Card + 5 children, Downtime Entry, Manufacturing Settings, Blanket Order + child (cross-module), Stock Entry manufacturing purposes.
- [Regional](modules/regional.md) — Regional-module inventory: directory layout, `regional_overrides` catalogue with purposes, `doc_events` per country, `@erpnext.allow_regional` stub slots, per-country deep dives (United Arab Emirates, Italy, United States, South Africa, Australia, Turkey, Saudi Arabia, France, Nepal), shared Italy FatturaPA inbound + outbound, UAE RCM flow, cross-module touchpoints. Mermaid diagrams for UAE PI submit with RCM, Italy SI submit outbound, FatturaPA inbound.
- [Regional DocType reference cards](modules/regional-doctypes.md) — per-doctype cards for Lower Deduction Certificate, Import Supplier Invoice, UAE VAT Settings + UAE VAT Account, South Africa VAT Settings, plus condensed per-country DocType shipping summary.
- [Assets](modules/assets.md) — Assets-module overview: `erpnext/assets/` directory layout, depreciation engine (`post_depreciation_entries` daily scheduler → `Asset Depreciation Schedule` row → JE per due row), depreciation methods (Straight Line / Double Declining Balance / Written Down Value / Manual; daily-prorata + shift-based sub-modes), multi-finance-book + mid-period rebuild via `reschedule_depreciation`, capitalization vs repair vs movement vs adjustment GL/SLE matrix, Asset lifecycle (Draft → Submitted → Partially / Fully Depreciated / Sold / Scrapped / Capitalized), Asset Maintenance scheduler, Asset Shift Allocation, all four Assets-related daily scheduler entries (`update_maintenance_status`, `make_post_gl_entry`, `update_asset_maintenance_log_status`, `post_depreciation_entries`), `@erpnext.allow_regional` open slots (`cancel_depreciation_entries`, `WDVMethod.get_wdv_or_dd_depr_amount`), cross-module ties to Buying (PI / PR auto-create Asset), Selling (SI disposal), Accounts (period closing + dimension registries).
- [Assets DocType reference cards](modules/assets-doctypes.md) — per-doctype cards for Asset, Asset Depreciation Schedule (+ Depreciation Schedule child + DepreciationScheduleController + StraightLineMethod / WDVMethod mixins), Asset Capitalization (+ Stock / Asset / Service Item children), Asset Repair (+ Consumed Item, Purchase Invoice children), Asset Movement (+ Movement Item child), Asset Value Adjustment, Asset Shift Allocation, Asset Maintenance (+ Task child + Team + Member child), Asset Maintenance Log, Asset Category (+ Category Account child), Asset Finance Book, Asset Shift Factor, Asset Activity, Location (NestedSet + GeoJSON area), Linked Location, plus the depreciation engine module functions catalogue.
- [Setup](modules/setup.md) — Setup-module overview: `erpnext/setup/` directory layout, the Company DocType heavy-lifter (`validate` 15-step chain, `on_update` mass-bootstrap of CoA + warehouses + cost centers + departments + per-country fixtures, `on_trash` cleanup), Setup Wizard 3+1 stages (`stage_fixtures` / `setup_company` / `setup_defaults` / optional `setup_demo`), Naming Series engine (`naming_series_variables` registry → 9 variables FY/TFY/ABBR/MM/DD/YY/YYYY/JJJ/WW resolved by `parse_naming_series_variable` in `accounts/utils.py`), Authorization Rule + AuthorizationControl(TransactionBase), Email Digest daily scheduler, Currency Exchange master + Currency Exchange Settings on-demand provider, Holiday List + the 6 NestedSet master groups (Item / Customer / Supplier / Sales Person / Territory / Department), Transaction Deletion Record 6-task workflow + Redis-cache `doc_events["*"].validate` guard, User → `validate_employee_role` employee-role auto-strip, demo loader (`erpnext/setup/demo.py` — create_demo_company / process_masters / make_transactions / convert_order_to_invoices). Open Question: no daily Currency Exchange refresh scheduler exists.
- [Setup DocType reference cards](modules/setup-doctypes.md) — per-doctype cards for Company (NestedSet, 1096 lines), Global Defaults (Single), Branch, the 6 NestedSet master groups, Employee + 3 work-history children + Employee Group + child + Designation, Holiday List + Holiday, Brand, Sales Partner (web), Quotation Lost Reason + child, Target Detail, Website Item Group, Vehicle, Driver, Driving License Category, Incoterm, Currency Exchange, Authorization Rule, Authorization Control (TransactionBase), Email Digest + recipient child, Transaction Deletion Record + 2 Setup-side children + Accounts-side details child, Terms and Conditions, UOM, UOM Conversion Factor, Party Type. Includes a module summary table (DocType / Class / Base / Tree / Hooks).
- [Support](modules/support.md) — Support-module overview: directory layout, the load-bearing wildcard `doc_events["*"].validate` SLA hook (`service_level_agreement.apply`) that runs on every doc save, Issue lifecycle (`Open / Replied / On Hold / Resolved / Closed`), SLA status machine + `agreement_status` truth table, working-hours-aware `calculate_first_response_time`, Communication ↔ Issue ↔ SLA wiring, `auto_close_tickets` (daily) + `check_agreement_status` (daily) schedulers, Warranty Claim handoff to Maintenance.
- [Support DocType reference cards](modules/support-doctypes.md) — per-doctype cards for Issue, Issue Type, Issue Priority, Service Level Agreement + 4 child tables (Service Day, Service Level Priority, SLA Fulfilled On Status, Pause SLA On Status), Support Settings (Single), Support Search Source, Warranty Claim.
- [Projects](modules/projects.md) — Projects-module overview: directory layout, Project lifecycle (`update_costing` aggregation from Timesheet/PI/SO/SI, `update_percent_complete` with 4 modes Manual/Task Completion/Task Progress/Task Weight), Task NestedSet (recursion guard, dependant rescheduling, daily `set_tasks_as_overdue`), Timesheet → Sales Invoice mapper, Project Update reminder cycle (Hourly / Twice Daily / Daily / Weekly with `collect_project_status` + `send_project_status_email_to_users`), six scheduler jobs total, calendar/portal surface, cross-module touch points (Selling / Buying / Stock / Manufacturing / Support / Setup).
- [Projects DocType reference cards](modules/projects-doctypes.md) — per-doctype cards for Project, Project Type, Project Update, Project Template + Project Template Task, Task + Task Type + Task Depends On + Dependent Task, Timesheet + Timesheet Detail, Activity Type + Activity Cost, Project User, Projects Settings (Single).
- [CRM](modules/crm.md) — CRM-module overview: directory layout, the surprising controller-base split (Lead extends `SellingController`, Opportunity extends `TransactionBase` only), Lead → Opportunity → Quotation conversion mapper chain, Prospect aggregator over Leads + Opportunities, doc_events wiring (Communication / Event / Contact / Email Unsubscribe), CRM Settings knob set, Contract status machine + fulfilment checklist, Email Campaign daily send pipeline + unsubscribe handling, five scheduler jobs, GDPR `user_privacy_documents` registration.
- [CRM DocType reference cards](modules/crm-doctypes.md) — per-doctype cards for Lead, Lead Source, Opportunity + Opportunity Item + Opportunity Type + Opportunity Lost Reason + Lost Reason Detail + Opportunity Lost Reason Detail, Prospect + Prospect Lead + Prospect Opportunity, Email Campaign + Campaign + Campaign Email Schedule, Competitor + Competitor Detail, Sales Stage, Market Segment, CRM Note, CRM Settings (Single), Appointment + Appointment Booking Settings + Appointment Booking Slots + Availability Of Slots, Contract + Contract Fulfilment Checklist + Contract Template + Contract Template Fulfilment Terms.
- [Quality Management](modules/quality-management.md) — Quality Management-module overview: directory layout, the all-plain-`Document` posture (no transaction-controller chain, no GL/SLE, none in cross-cutting registries), Quality Procedure NestedSet with parent/child sync, Quality Goal → Quality Review daily cadence (Daily / Weekly / Monthly / Quarterly), Non Conformance → Quality Action workflow, Quality Feedback multi-parameter rating (auto-template clone, self-rating default), single scheduler job (`quality_review.review`).
- [Quality Management DocType reference cards](modules/quality-management-doctypes.md) — per-doctype cards for Quality Goal + Quality Goal Objective, Quality Procedure (NestedSet) + Quality Procedure Process, Quality Action + Quality Action Resolution, Quality Meeting + Quality Meeting Agenda + Quality Meeting Minutes, Quality Review + Quality Review Objective, Non Conformance, Quality Feedback + Quality Feedback Parameter, Quality Feedback Template + Quality Feedback Template Parameter.
- [Maintenance](modules/maintenance.md) — Maintenance-module overview: directory layout (no workspace, no web_form), Maintenance Schedule generation (per-item × periodicity → `Maintenance Schedule Detail` rows with holiday-skipped dates), Sales-Person `frappe.Event` calendar creation on submit, Serial No `amc_expiry_date` propagation bridge to Stock's `update_maintenance_status`, Maintenance Visit submit/cancel writeback to Schedule Detail and Warranty Claim (`Fully Completed → Closed` mapping, cancel reverts to next-most-recent partial visit), `check_if_last_visit` cancel guard, **no scheduler jobs** of its own.
- [Maintenance DocType reference cards](modules/maintenance-doctypes.md) — per-doctype cards for Maintenance Schedule + Maintenance Schedule Item + Maintenance Schedule Detail, Maintenance Visit + Maintenance Visit Purpose.
- [Portal](modules/portal.md) — Portal-module overview: `on_session_creation` auto-provisions Customer / Supplier on first login of a Website User; `set_default_role` keeps roles in sync on User on_update; `website_route_rules` (14 entries) maps every customer/supplier-portal URL onto a back-end DocType; `standard_portal_menu_items` (14 entries) defines the role-gated sidebar; `has_website_permission` (11 transactional DocTypes) routes record visibility through `controllers/website_list_for_contact.py`. Module's own DocTypes are 2 child tables (Website Attribute, Website Filter Field) used by storefront filtering.
- [Portal DocType reference cards](modules/portal-doctypes.md) — per-doctype cards for Website Attribute, Website Filter Field.
- [Shopping Cart](modules/shopping-cart.md) — Shopping Cart-module overview: at this commit the in-tree module is **a shell** (3 empty `__init__.py` files); cart and storefront DocTypes have moved to the out-of-tree `webshop` app. The only live in-tree wiring is `payment_gateway_enabled = "erpnext.accounts.utils.create_payment_gateway_account"` ([hooks.py:521](../erpnext/hooks.py:521)) called by the external `payments` app to materialise a GL Account when a Payment Gateway is enabled. Cart-flow DocTypes (Quotation, Sales Order, Customer, Address, Payment Request) live in their respective transactional modules.
- [Shopping Cart DocType reference cards](modules/shopping-cart-doctypes.md) — confirms zero in-tree DocTypes; cross-pointer table to where cart-related DocTypes actually live.
- [`www/` and `templates/`](modules/www-and-templates.md) — public-page surface inventory: 8 `www/` slots (only `book_appointment/`, `support/`, `payment_setup_certification` are live; 5 are empty placeholders) plus 6 `templates/` subdirs (emails / form_grid / generators / includes / pages / print_formats) plus `templates/utils.py` `send_message` override. Hooks: `override_whitelisted_methods` ([hooks.py:58](../erpnext/hooks.py:58)), `webform_list_context` ([hooks.py:109](../erpnext/hooks.py:109)), `website_generators` ([hooks.py:113](../erpnext/hooks.py:113)), `website_route_rules` ([hooks.py:121-218](../erpnext/hooks.py:121)). No DocTypes ship under either tree.
- [Utilities](modules/utilities.md) — Utilities-module overview: cross-cutting helpers — `TransactionBase(StatusUpdater)` (the controller-chain layer between `StatusUpdater` and `AccountsController`), `bulk_transaction.py` engine (8-source × N-target conversion mapper + savepoint-isolated retry on `hourly_maintenance`), `activation.py` (24-doctype scoring driving the desk help bubble via `get_help_messages` hook), `__init__.get_site_info` (anonymous usage stats), `__init__.payment_app_import_guard` context manager, `product.get_price` / `get_item_codes_by_attributes` (storefront pricing), `naming.set_by_naming_series` (Setup Wizard helper), `regional.temporary_flag` context manager. **`bot_parsers = ["erpnext.utilities.bot.FindItemBot"]` ([hooks.py:516](../erpnext/hooks.py:516)) registers a target whose file does not exist on disk** — TODO(verify).
- [Utilities DocType reference cards](modules/utilities-doctypes.md) — per-doctype cards for Video, Video Settings, Rename Tool, Portal User.
- [Bulk Transaction](modules/bulk-transaction.md) — Bulk Transaction-module overview: ships only the two log DocTypes; the engine itself lives in [erpnext/utilities/bulk_transaction.py](../erpnext/utilities/bulk_transaction.py). `Bulk Transaction Log` is a **virtual** DocType aggregating Detail rows by date. Hourly retry scheduler (`utilities.bulk_transaction.retry`) re-runs failed rows of today's date with savepoint isolation and `retried` flag de-duplication.
- [Bulk Transaction DocType reference cards](modules/bulk-transaction-doctypes.md) — per-doctype cards for Bulk Transaction Log (virtual) + Bulk Transaction Log Detail.
- [ERPNext Integrations](modules/erpnext-integrations.md) — ERPNext Integrations-module overview: thin module whose only first-party integration is **Plaid bank-feed sync**. `automatic_synchronization` (`hourly_maintenance`, [hooks.py:457](../erpnext/hooks.py:457)) fans out per-account sync jobs that exchange Plaid public token → access token, fetch transactions since `last_integration_date` (or 12 months ago if unset), and submit `Bank Transaction` records (positive amount → withdrawal, negative → deposit). Helper module `utils.py` ships HMAC webhook validation, webhook URL builder, parcel-tracking URL formatter. `custom/contact.json` Customize Form fixture adds Plaid-related fields to Contact.
- [ERPNext Integrations DocType reference cards](modules/erpnext-integrations-doctypes.md) — per-doctype card for Plaid Settings (Single).
- [Telephony](modules/telephony.md) — Telephony-module overview: 5 DocTypes around an inbound/outbound `Call Log` model. `Call Log` lifecycle (validate / before_insert / after_insert / on_update) auto-links Contact + Lead by phone, sets `call_received_by` from Employee, fires desk popup via `frappe.publish_realtime("show_call_popup", user=email)` to scheduled employees, and emits `call_<id>_missed` / `call_<id>_ended` realtime events on status transitions. Two universal hooks: `additional_timeline_content["*"]` ([hooks.py:684](../erpnext/hooks.py:684)) injects linked-call cards into **every doctype's** timeline; Contact `after_insert` ([hooks.py:400](../erpnext/hooks.py:400)) back-fills Dynamic Links on existing Call Logs. `Incoming Call Settings` validates per-day timeslot ordering and overlap; `sounds` registers 3 desk audio assets. **No scheduler jobs.**
- [Telephony DocType reference cards](modules/telephony-doctypes.md) — per-doctype cards for Call Log, Incoming Call Settings, Incoming Call Handling Schedule, Voice Call Settings, Telephony Call Type.
- [Communication](modules/communication.md) — Communication-module overview: distinct from Frappe core's `Communication` DocType. Ships only **two** DocTypes — `Communication Medium` (Voice / Email / Chat) and child `Communication Medium Timeslot` (per-day Employee Group on-duty roster). Pure schema, no methods. `doc_events["Communication"]` wiring at [hooks.py:362-371](../erpnext/hooks.py:362) dispatches into Support (SLA + Issue first-response) and CRM (Prospect linking) — those handlers live in **other modules**, not here.
- [Communication DocType reference cards](modules/communication-doctypes.md) — per-doctype cards for Communication Medium + Communication Medium Timeslot.
- [EDI](modules/edi.md) — EDI-module overview: new and intentionally narrow v17 module. Two DocTypes — `Code List` (versioned vocabulary; cascades Common Codes on trash) and `Common Code` (per-code record with Dynamic Link `applies_to` table; `validate_distinct_references` enforces one code per `(code_list, link_doctype, link_name)`). `from_genericode` extractors + module-level `import_genericode` consumer drive the genericode-XML import button injected via `doctype_list_js` ([hooks.py:45-52](../erpnext/hooks.py:45)). DDL: `on_doctype_update` adds a composite index `(code_list, common_code)`. No GL/SLE impact, no scheduler jobs, no consumer wiring at this commit.
- [EDI DocType reference cards](modules/edi-doctypes.md) — per-doctype cards for Code List + Common Code.

## ADRs

- [ADR index](adr/README.md) — convention, section template, full table of accepted decisions.
- [ADR 0001 — Immutable ledger via `auto_cancel_exempted_doctypes` and reverse entries](adr/0001-immutable-ledger.md) — GL Entry / SLE / PLE / Account Closing Balance are exempted from auto-cancel; cancel writes reverse rows with `is_cancelled=1` instead of mutating originals.
- [ADR 0002 — `@erpnext.allow_regional` + `regional_overrides` registry over country subclasses](adr/0002-regional-overrides-pattern.md) — regionalization uses a hooks-dict + decorator pattern (per-company country routing, last-installed-app wins) instead of per-country subclasses or monkey-patching.
- [ADR 0003 — Manufacturing DocTypes extend `Document` directly; Stock Entry carries GL/SLE](adr/0003-no-manufacturing-controller.md) — Production Plan / Work Order / Job Card / BOM stay as orchestration objects; all stock movement and GL impact for manufacturing flows through Stock Entry.
- [ADR 0004 — `payments` app installed before `erpnext`](adr/0004-payments-app-dependency.md) — gateway code was extracted into a standalone `payments` app reusable by other Frappe apps; ERPNext now depends on it (`bench get-app payments && bench get-app erpnext`).
- [ADR 0005 — Declarative `status_updater[]` for cross-doc status propagation](adr/0005-status-updater-vs-doc-events.md) — cross-document cascades use a class-attribute list-of-dicts consumed by `update_prevdoc_status`; `doc_events` is reserved for cross-cutting wildcards.
- [ADR 0006 — `is_old_subcontracting_flow=1` preserves legacy PO/PR-embedded flow alongside new SCO/SCR](adr/0006-subcontracting-v15-coexistence.md) — v15's Subcontracting Order / Receipt coexist with the legacy flow indefinitely; a single `SubcontractingController` dispatches via `subcontract_data` based on the per-document flag.

## Writing conventions

- Every architectural claim cites `[name](path:line)`.
- Mermaid diagrams use `TD` for hierarchies and `LR` / `sequenceDiagram` for flows.
- Solid arrows are synchronous calls; dashed arrows are event-based / registration-time wiring.
- Each document's frontmatter carries `last_updated`, `commit`, `scope`, and `status`.
